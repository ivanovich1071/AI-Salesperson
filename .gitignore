# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env / secrets
.env
.env.local
.env.production
# доступы к серверу (пароли/ключи) — НИКОГДА не коммитить (репозиторий публичный)
DEPLOY_SECRETS.local.md

# рабочие заметки (не часть приложения)
Лаборатория_решений_план.txt

# database
/prisma/*.db
/prisma/*.db-journal

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# claude local settings (содержит локальные разрешения)
.claude/settings.local.json

# obsidian: локальное состояние рабочего пространства (конфиг vault'а — в git)
knowledge/.obsidian/workspace.json
knowledge/.obsidian/workspace-mobile.json
knowledge/.obsidian/cache
knowledge/.obsidian/plugins
